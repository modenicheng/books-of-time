# ── 行尾统一为 LF ──────────────────────────────────────────
# 所有文本文件自动检测并统一使用 LF 行尾（git 标准）
* text=auto eol=lf

# ── 二进制文件例外（不做行尾转换） ────────────────────────
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
*.eot  binary
*.ttf  binary
*.otf  binary
*.gz   binary
*.zip  binary
*.tar  binary
